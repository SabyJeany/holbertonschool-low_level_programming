#include "main.h"
#include <stdio.h>

int main(void)
{
    printf("%d\n", _pow_recursion(2, 3));
    printf("%d\n", _pow_recursion(5, 0));
    printf("%d\n", _pow_recursion(2, -2));
    return (0);
}
